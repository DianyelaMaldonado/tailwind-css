/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./src/**/*.{html,js}"], //tailwind trabaja con un elemento llamdo -> jit(just in time -permite agregar clases personalizadas directo en el html y me permite cargarlas automaticamente en la hoja de estilos)
  theme: {
    extend: {},
  },
  plugins: [],
}

